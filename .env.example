# Copy to .env and fill in. .env is git-ignored.

# The Odds API key (free tier at https://the-odds-api.com).
# Setting it here keeps the key out of the browser entirely — the Vegas tab
# will show "using the key set on the server" and you can leave the field blank.
ODDS_API_KEY=
